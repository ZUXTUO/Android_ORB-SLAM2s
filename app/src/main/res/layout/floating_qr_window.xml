<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/floating_qr_card"
    android:layout_width="200dp"
    android:layout_height="250dp"
    android:layout_marginTop="60dp"
    android:layout_marginStart="16dp"
    app:cardBackgroundColor="#D937474F"
    app:cardCornerRadius="8dp"
    app:cardElevation="6dp"
    android:visibility="gone">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <RelativeLayout
            android:id="@+id/qr_window_header"
            android:layout_width="match_parent"
            android:layout_height="32dp"
            android:background="#9900A9DE"
            android:paddingStart="8dp"
            android:paddingEnd="8dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:layout_toStartOf="@id/btn_close_qr"
                android:maxLines="1"
                android:ellipsize="end"
                android:text="@string/title_web_server"
                android:textColor="#FFFFFF"
                android:textStyle="bold"
                android:textSize="12sp" />

            <ImageView
                android:id="@+id/btn_close_qr"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:padding="2dp"
                android:src="@drawable/ic_close"
                app:tint="#CCCCCC"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/btn_close_qr" />
        </RelativeLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="12dp">

            <ImageView
                android:id="@+id/iv_qr_code"
                android:layout_width="160dp"
                android:layout_height="160dp"
                android:scaleType="fitCenter"
                android:background="@drawable/bg_glass_white"
                android:padding="8dp"
                android:contentDescription="@string/qr_code_desc" />

            <TextView
                android:id="@+id/tv_web_url"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="@string/web_url_default"
                android:textColor="#FFFFFF"
                android:textSize="11sp"
                android:gravity="center"
                android:textIsSelectable="true"/>

        </LinearLayout>

    </LinearLayout>
</androidx.cardview.widget.CardView>
